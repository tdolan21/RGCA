// var sequelize = require("../scripts/seeds.js");

// var orm = {
//     selectAll: function (cb) {  // added a callback as the method's *last* parameter
//         var queryString = "SELECT * FROM rgca_db";
//         connection.query(queryString, function (err ,result){
//             if (err) {
//                 throw err;
//             }
//             // console.log(result);
//             cb(result);  // passed connection.query's result output into the callback
//         });
//     }
